#if defined _preview_included
	#endinput
#endif
#define _preview_included

native preview_menu(id);
native preview_submenu_players(id);
native preview_butchers(id);
native preview_knifes(id);
native preview_players_ct(id);
native preview_players_tr(id);